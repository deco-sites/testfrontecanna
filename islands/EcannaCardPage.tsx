import Component from "$store/components/ui/EcannaCardPage.tsx";
import type { Props } from "$store/components/ui/EcannaCardPage.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
