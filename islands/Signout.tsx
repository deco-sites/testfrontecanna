import Component from "$store/components/ui/Signout.tsx";
import type { Props } from "$store/components/ui/Signout.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
