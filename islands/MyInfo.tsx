import Component from "$store/components/ui/MyInfo.tsx";
// import type { Props } from "$store/components/ui/MyInfo.tsx";

function Island() {
  return <Component />;
  // return <Component {...props} />;
}

export default Island;
