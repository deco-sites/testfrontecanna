import Component from "$store/components/ui/CheckoutForm.tsx";
import type { Props } from "$store/components/ui/CheckoutForm.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
