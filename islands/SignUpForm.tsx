import Component from "$store/components/ui/SignUpForm.tsx";
import type { Props } from "$store/components/ui/SignUpForm.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
