import Component from "$store/components/ui/SignInForm.tsx";
import type { Props } from "$store/components/ui/SignInForm.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
