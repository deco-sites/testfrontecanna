import Component from "$store/components/ui/PrivatePageControl.tsx";
import type { Props } from "$store/components/ui/PrivatePageControl.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
